---
title: check_for_unique_units
pagefind: true
---

This function takes two vectors, one for parameters and one for units and determines if the parameters only have unique units.

## Arguments
* `params`: vector of parameter names
* `units`: vector of units associated with each parameter

## Returns
* `TRUE` if params only have one unit
* `FALSE` otherwise

