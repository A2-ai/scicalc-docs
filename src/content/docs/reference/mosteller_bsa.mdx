---
title: "Calculate Body Surface Area Using Mosteller Equation"
description: "Calculate Body Surface Area Using Mosteller Equation"
---

## Description

Calculate Body Surface Area Using Mosteller Equation

## Usage

```r
mosteller_bsa(weight, height)
```

## Arguments

| Argument | Description |
|----------|-------------|
| `weight` | weight of subject (kg) |
| `height` | height of subject (cm) |

## Details

The Mosteller equation for BSA: 

$$
BSA = \sqrt{\frac{W \cdot H}{3600}}
$$

 where: 
- $W$ = weight (kg)
- $H$ = height (cm)

## Value

the body surface area (m^2)

## See Also

Other body_composition: 
<a href={`${import.meta.env.BASE_URL}reference/aibw`}><code>aibw</code></a>,
<a href={`${import.meta.env.BASE_URL}reference/bmi`}><code>bmi</code></a>,
<a href={`${import.meta.env.BASE_URL}reference/bmic`}><code>bmic</code></a>,
<a href={`${import.meta.env.BASE_URL}reference/bsa`}><code>bsa</code></a>,
<a href={`${import.meta.env.BASE_URL}reference/dubois_bsa`}><code>dubois_bsa</code></a>,
<a href={`${import.meta.env.BASE_URL}reference/ibw`}><code>ibw</code></a>

## Examples

```r
mosteller_bsa(70, 170)
```

### Output

import exampleOutput from '/examples/text/mosteller_bsa.txt?raw';

<pre style="overflow-x: auto;">{exampleOutput}</pre>
