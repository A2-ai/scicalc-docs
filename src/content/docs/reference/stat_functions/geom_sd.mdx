---
title: geom_sd
pagefind: true
---

import { Content as EContent } from '../../../../content/rd_docs/geom_sd.md';

<EContent />

## Equation
$$
\text{geometric }\sigma =
    \exp{ \left(
        \sqrt{
            \text{Var} \left[ 
                \log(x)
            \right]
        }
        \right)
    }
$$

## Code
```r
geom_sd = exp(stats::sd(log(x), na.rm = na.rm))
```