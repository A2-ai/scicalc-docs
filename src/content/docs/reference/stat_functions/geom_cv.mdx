---
title: geom_cv
pagefind: true
---
import { Content as EContent } from '../../../../content/rd_docs/geom_cv.md';

<EContent />
import { Code } from '@astrojs/starlight/components';
import importedCode from '/src/assets/output/geom_cv.txt?raw';

<Code code={importedCode} lang="r" title="geom_cv.R" />

## Equation
$$
\text{geometric }CV = \sqrt{
    \exp{\left(
        \text{Var} \left[ \log(x)\right]
        \right)
    } - 1
}
$$

## Code
```r
geom_cv = sqrt(exp(stats::sd(log(x), na.rm = na.rm)**2) - 1)
```
