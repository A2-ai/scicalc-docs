---
title: "Calculate Geometric Coefficient of Variation"
description: "Calculate Geometric Coefficient of Variation"
---

## Description

Calculate Geometric Coefficient of Variation

## Usage

```r
geom_cv(x, na.rm = FALSE)
```

## Arguments

| Argument | Description |
|----------|-------------|
| `x` | vector of data you want the geometric CV of. |
| `na.rm` | boolean to remove NA from vector. Default is FALSE |

## Details

The geometric coefficient of variation is calculated as: 

$$
GCV = \sqrt{\exp(Var[\log(x)]) - 1}
$$

## Value

the geometric CV of the input vector x

## See Also

Other statistics: 
<a href={`${import.meta.env.BASE_URL}reference/cor_df`}><code>cor_df</code></a>,
<a href={`${import.meta.env.BASE_URL}reference/cv`}><code>cv</code></a>,
<a href={`${import.meta.env.BASE_URL}reference/geom_mean`}><code>geom_mean</code></a>,
<a href={`${import.meta.env.BASE_URL}reference/geom_sd`}><code>geom_sd</code></a>

## Examples

```r
geom_cv(c(1, 2, 3, 2, 1))
```

### Output

import exampleOutput from '/examples/text/geom_cv.txt?raw';

<pre style="overflow-x: auto;">{exampleOutput}</pre>
