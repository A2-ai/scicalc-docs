---
title: is_asian
pagefind: true
---

This function takes character input and returns TRUE/FALSE if asain/other

## Arguments
* `x`: character representing race

## Returns 
* `TRUE` if `tolower(x)` is `"asian"`
* `FALSE` otherwise
