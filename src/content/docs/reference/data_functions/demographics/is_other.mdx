---
title: is_other
pagefind: true
---

This function takes character input and returns TRUE/FALSE if other/specified race

## Arguments
* `x`: character representing race

## Returns 
* `TRUE` if first letter of `tolower(x)` is `"other"`
* `FALSE` otherwise