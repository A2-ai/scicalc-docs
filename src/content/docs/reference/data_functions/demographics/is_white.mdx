---
title: is_white
pagefind: true
---

This function takes character input and returns TRUE/FALSE if white/other

## Arguments
* `x`: character representing race

## Returns 
* `TRUE` if first letter of `tolower(x)` is `"white"`
* `FALSE` otherwise