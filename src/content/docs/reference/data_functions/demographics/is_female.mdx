---
title: is_female
pagefind: true
---

This function takes character input and returns TRUE/FALSE if female/other

## Arguments
* `x`: character representing sex

## Returns 
* `TRUE` if first letter of `tolower(x)` is `"f"`
* `FALSE` otherwise