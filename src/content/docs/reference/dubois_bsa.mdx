---
title: "Calculate Body Surface Area Using Du Bois Equation"
description: "Calculate Body Surface Area Using Du Bois Equation"
---

## Description

Calculate Body Surface Area Using Du Bois Equation

## Usage

```r
dubois_bsa(weight, height)
```

## Arguments

| Argument | Description |
|----------|-------------|
| `weight` | weight of subject (kg) |
| `height` | height of subject (cm) |

## Details

The Du Bois equation for BSA: 

$$
BSA = 0.007184 \cdot W^{0.425} \cdot H^{0.725}
$$

 where: 
- $W$ = weight (kg)
- $H$ = height (cm)

## Value

the body surface area (m^2)

## See Also

Other body_composition: 
<a href={`${import.meta.env.BASE_URL}reference/aibw`}><code>aibw</code></a>,
<a href={`${import.meta.env.BASE_URL}reference/bmi`}><code>bmi</code></a>,
<a href={`${import.meta.env.BASE_URL}reference/bmic`}><code>bmic</code></a>,
<a href={`${import.meta.env.BASE_URL}reference/bsa`}><code>bsa</code></a>,
<a href={`${import.meta.env.BASE_URL}reference/ibw`}><code>ibw</code></a>,
<a href={`${import.meta.env.BASE_URL}reference/mosteller_bsa`}><code>mosteller_bsa</code></a>

## Examples

```r
b <- dubois_bsa(80.56, 167)

df <- data.frame(
"WT" = c(80.56, 71.53, 81.04, 70.17),
"HT" = c(167, 161, 163, 164)
)
df <- dplyr::mutate(df, bsa = dubois_bsa(WT, HT))
df
```

### Output

import exampleOutput from '/examples/text/dubois_bsa.txt?raw';

<pre style="overflow-x: auto;">{exampleOutput}</pre>
