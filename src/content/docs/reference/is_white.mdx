---
title: "Check if Race is White"
description: "Check if Race is White"
---

## Description

Check if Race is White

## Usage

```r
is_white(x)
```

## Arguments

| Argument | Description |
|----------|-------------|
| `x` | input character representing race |

## Value

boolean representing Race == White

## See Also

Other demographics: 
<a href={`${import.meta.env.BASE_URL}reference/ethnicn`}><code>ethnicn</code></a>,
<a href={`${import.meta.env.BASE_URL}reference/is_asian`}><code>is_asian</code></a>,
<a href={`${import.meta.env.BASE_URL}reference/is_black`}><code>is_black</code></a>,
<a href={`${import.meta.env.BASE_URL}reference/is_female`}><code>is_female</code></a>,
<a href={`${import.meta.env.BASE_URL}reference/is_hispanic_or_latino`}><code>is_hispanic_or_latino</code></a>,
<a href={`${import.meta.env.BASE_URL}reference/is_not_hispanic_or_latino`}><code>is_not_hispanic_or_latino</code></a>,
<a href={`${import.meta.env.BASE_URL}reference/is_other`}><code>is_other</code></a>,
<a href={`${import.meta.env.BASE_URL}reference/racen`}><code>racen</code></a>,
<a href={`${import.meta.env.BASE_URL}reference/sexf`}><code>sexf</code></a>

## Examples

```r
is_white("WHITE")

is_white("BLACK")

is_white(1)
```

### Output

import exampleOutput from '/examples/text/is_white.txt?raw';

<pre style="overflow-x: auto;">{exampleOutput}</pre>
