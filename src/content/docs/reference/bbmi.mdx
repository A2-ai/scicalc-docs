---
title: "Calculate BMI (Deprecated) bbmi() was renamed to bmi() to improve function naming consistency."
description: "Calculate BMI (Deprecated) bbmi() was renamed to bmi() to improve function naming consistency."
---

`bbmi()` was renamed to `bmi()` to improve function naming consistency.

## Description

Calculate BMI (Deprecated)

`bbmi()` was renamed to `bmi()` to improve function naming consistency.

## Usage

```r
bbmi(...)
```

## Arguments

|       |                             |
|-------|-----------------------------|
| `...` | Arguments passed to `bmi()` |
