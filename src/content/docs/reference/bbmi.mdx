---
title: "Calculate BMI (Deprecated)"
description: "bbmi() was renamed to bmi() to improve function naming consistency."
---

## Description

`bbmi()` was renamed to `bmi()` to improve function naming consistency.

## Usage

```r
bbmi(...)
```

## Arguments

|       |                             |
|-------|-----------------------------|
| `...` | Arguments passed to `bmi()` |
